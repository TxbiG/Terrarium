# TerrariumOS direct boot for ARM64 boards and UEFI ARM systems.
mode=single
timeout_ms=0
default=0

[entry]
name=TerrariumOS
arch=aarch64
protocol=efi-stub
kernel=/boot/aarch64/terrarium-kernel.efi
initrd=/boot/initrd.img
cmdline=root=auto devicetree=auto
